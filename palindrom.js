/**
 * @param {string} s
 * @return {string}
 */
 var longestPalindrome = function(s) {
    if(s.length <=1) return s;
    let pointerA = 0;
    let pointerB = s.length -1;

    while(pointerA >= s.length -1 || pointerB >= 1){
        
    }

    
};

let returnString = (array)=>{



    return;
}

console.log(longestPalindrome('babad'))